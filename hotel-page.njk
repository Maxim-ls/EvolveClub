---
pagination:
  data: collections.hotel
  size: 1
  alias: hotel
permalink: "{% if hotel.data.published !== false %}/hotels/{{ hotel.data.country }}/{{ hotel.data.slug }}/{% else %}false{% endif %}"
layout: layouts/base.njk
title: "{{ hotel.data.seo_title or hotel.data.title + ' - EvolveClub' }}"
description: "{{ hotel.data.seo_description or hotel.data.subtitle }}"
bodyClass: "country-page hotel-detail-page"
---

<main id="top">
  <section class="hotel-detail-hero" style="--hotel-hero-image: url('{{ hotel.data.cover }}'); --hotel-next-image: url('{{ hotel.data.cover }}');">
    <div class="hotel-detail-inner">
      <a class="country-back" data-history-back href="/countries-test/{{ hotel.data.country }}/#hotels"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M15 18l-6-6 6-6"/></svg> Назад к отелям</a>
      <span class="hotel-stars" aria-label="Отель"><span class="hotel-rating-label">
        {% for segment in hotel.data.segments %}
          {{ segmentLabels[segment] or segment }}{% if not loop.last %} / {% endif %}
        {% endfor %}
      </span></span>
      <h1>{{ hotel.data.title }}</h1>
      <span class="hotel-location"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true"><path d="M20 10c0 4.993-5.539 10.193-7.399 11.799a1 1 0 0 1-1.202 0C9.539 20.193 4 14.993 4 10a8 8 0 0 1 16 0Z"/><circle cx="12" cy="10" r="3"/></svg>{{ hotel.data.country }}</span>
    </div>
    <div class="hotel-hero-actions" aria-label="Заявка по отелю">
      <a class="primary-button" href="/#request">Оставить заявку</a>
    </div>
  </section>

  <section class="hotel-detail-main">
    <div class="hotel-meta-grid">
      <div class="hotel-info-box">
        <span>Страна</span>
        <p>{{ hotel.data.country }}</p>
      </div>
      <div class="hotel-info-box">
        <span>Сегменты</span>
        <p>
          {% for segment in hotel.data.segments %}
            {{ segmentLabels[segment] or segment }}{% if not loop.last %}, {% endif %}
          {% endfor %}
        </p>
      </div>
    </div>

    <section class="hotel-content-section hotel-about-feature">
      <div>
        <span>Об отеле</span>
        <h2>{{ hotel.data.subtitle }}</h2>
        {{ hotel.templateContent | safe }}
      </div>
      <ul class="hotel-facts">
        {% for segment in hotel.data.segments %}
          <li>
            <span>{{ segmentLabels[segment] or segment }}</span>
            <p>Сегмент отеля</p>
          </li>
        {% endfor %}
      </ul>
    </section>

    <section class="hotel-content-section">
      <span>Фото</span>
      <h2>Галерея</h2>
      <div class="hotel-photo-mosaic">
        {% if hotel.data.gallery and hotel.data.gallery.length %}
          {% for item in hotel.data.gallery %}
            <img src="{{ item.image or item }}" alt="{{ hotel.data.title }}" loading="lazy">
          {% endfor %}
        {% else %}
          <img src="{{ hotel.data.cover }}" alt="{{ hotel.data.title }}" loading="lazy">
        {% endif %}
      </div>
    </section>

    {% if hotel.data.rooms and hotel.data.rooms.length %}
      <section class="hotel-content-section">
        <span>Размещение</span>
        <h2>Номера и виллы</h2>
        <div class="room-grid room-grid-detailed">
          {% for room in hotel.data.rooms %}
            {% set roomImages = [] %}
            {% if room.images and room.images.length %}
              {% for item in room.images %}
                {% set roomImages = (roomImages.push(item.image or item), roomImages) %}
              {% endfor %}
            {% endif %}
            {% set firstImage = roomImages[0] or hotel.data.cover %}
            <article class="room-card">
              <div class="room-gallery" data-room-gallery data-room-images="{{ roomImages | join('|') }}">
                <img src="{{ firstImage }}" alt="{{ room.title }}" loading="lazy">
                <button type="button" data-room-prev aria-label="Предыдущее фото"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M15 18l-6-6 6-6"/></svg></button>
                <button type="button" data-room-next aria-label="Следующее фото"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M9 6l6 6-6 6"/></svg></button>
              </div>
              {% if room.type %}
                <span>{{ room.type }}</span>
              {% endif %}
              <h3>{{ room.title }}</h3>
              <p>{{ room.description }}</p>
              {% if room.area or room.capacity %}
                <p>
                  {% if room.area %}<b>{{ room.area }}</b>{% endif %}
                  {% if room.area and room.capacity %} · {% endif %}
                  {% if room.capacity %}{{ room.capacity }}{% endif %}
                </p>
              {% endif %}
            </article>
          {% endfor %}
        </div>
      </section>
    {% endif %}
  </section>
</main>
